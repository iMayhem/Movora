name: movora
description: Your ultimate destination for movies and TV shows.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  cached_network_image: ^3.3.0
  # carousel_slider: ^4.2.1 - removed due to compatibility issues
  flutter_staggered_grid_view: ^0.7.0
  shimmer: ^3.0.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.1
  flutter_svg: ^2.0.9
  intl: ^0.19.0
  webview_flutter: ^4.7.0
  webview_flutter_android: ^3.16.0
  js: ^0.6.7
  package_info_plus: ^8.0.2
  mixpanel_flutter: ^2.4.4
  video_player: ^2.8.2
  chewie: ^1.7.4
  # firebase_core: ^3.15.2 - removed
  # firebase_auth: ^5.3.0 - removed
  # google_sign_in: ^6.2.1 - removed
  # firebase_analytics: ^10.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
  
  # Fonts will be added later - using system fonts for now

